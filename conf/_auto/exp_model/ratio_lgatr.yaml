exp:
  _target_: experiments.particles.ratios.ExperimentRatiosParticles
model:
  _target_: wrappers.lgatr_wrapper.ParametrizedLGATrWrapper
  net:
    # θ is routed through LGATr's scalar pathway (see derive_config.py), so
    # the multivector pathway only carries particle 4-vectors.
    in_mv_channels: 1
    out_mv_channels: 1
