# Dataset for the one-dimensional study
# Full path will depend on whether we are doing 'particles' or 'features'
# so derive dynamically
defaults:
  - _base

key: 1d
base: /data/atlas/users/qvanenge/experiment_so_cht
events_file: ${.path}/experiment_so_cht.h5
theta_dim: 1