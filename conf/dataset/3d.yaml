# Dataset for the three-dimensional study
# Full path will depend on whether we are doing 'particles' or 'features'
# so derive dynamically
defaults:
  - _base

key: 3d
base: data/atlas/users/qvanenge/experiment_so_cht_ctw_ctb
events_file: ${.path}/experiment_so_cht_ctw_ctb.h5
theta_dim: 3